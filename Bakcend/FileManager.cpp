#include <string>
#include <vector>
#include "FileManager.h"

std::vector<std::string> __stdcall FileManager::readDirectory(std::string directory) {
	std::vector<std::string> files = {};

	return files;
}